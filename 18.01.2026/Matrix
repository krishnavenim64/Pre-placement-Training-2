class Solution {
    public int[][] matrixReshape(int[][] mat, int r, int c) {
        int m = mat.length, n = mat[0].length;
        if (m*n != r*c) return mat;
        int[][] res = new int[r][c];
        int k = 0;
        for (int[] row : mat)
            for (int val : row)
                res[k/c][k++%c] = val;
        return res;
    }
}
