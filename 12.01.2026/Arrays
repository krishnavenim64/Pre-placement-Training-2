def twoSum(nums, target):
    dic = {}
    for i, num in enumerate(nums):
        diff = target - num
        if diff in dic:
            return [dic[diff], i]
        dic[num] = i

# Example
nums = [2, 7, 11, 15]
target = 9
print(twoSum(nums, target))
